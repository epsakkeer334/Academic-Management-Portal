<div class="col-md-6">
    <div class="card shadow-sm border-0">
        <div class="card-body">
            <h6 class="card-title">Institute Admin</h6>
            <p class="small text-muted">Manage your institute settings and users.</p>
            <div class="d-flex gap-2 flex-wrap">
                <a href="{{ route('admin.institutes') }}" class="btn btn-sm btn-primary">Institutes</a>
                <a href="{{ route('admin.students') }}" class="btn btn-sm btn-outline-secondary">Students</a>
                <a href="{{ route('admin.courses') }}" class="btn btn-sm btn-outline-info">Courses</a>
            </div>
        </div>
    </div>
</div>
